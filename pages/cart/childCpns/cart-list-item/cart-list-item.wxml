<!--pages/cart/childCpns/cart-list-item/cart-list-item.wxml-->
<view class="cart-item">
  <view class="item-check">
    <g-check-icon isChecked='{{goods.checked}}' bindtap="onCheckClick" data-index="{{index}}"/>
  </view>
  <view class="item-image">
    <image src="{{goods.image}}" class="picture" mode="aspectFill"/>
  </view>
  <view class="item-info">
    <view class="title">{{goods.title}}</view>
    <view class="desc">{{goods.desc}}</view>
    <view class="price-count">
      <view class="price">￥{{goods.price}}</view>
      <view class="count">X{{goods.count}}</view>
    </view>
  </view>
</view>
